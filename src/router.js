// 导入vue
import vue from 'vue'

// 导入 vue-router
import VueRouter from 'vue-router';


// 创建路由对象 
const router = new VueRouter({
    // 配置路由规则
    routes: [
        { name: '' }
    ]


});

// 导出路由
export default router;